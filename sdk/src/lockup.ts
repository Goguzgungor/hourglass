// NOTE: tsc emits `src/generated/lockup/src/index.ts` as
// `dist/generated/lockup/src/index.js`, NOT under a `dist/` subfolder, so the
// relative path here must match that layout.
export * from './generated/lockup/src/index.js';
