[workspace]
resolver = "2"
members = [
    "contracts/shared",
    "contracts/comptroller",
    "contracts/lockup",
]

[workspace.package]
version = "0.2.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/hourglass-protocol/hourglass"
rust-version = "1.79"

[workspace.dependencies]
# Pin to soroban-sdk 25.3 because stellar-tokens 0.7.1 hard-pins it; mixing
# 25/26 in the dependency graph breaks the macro-generated trait machinery.
soroban-sdk = "=25.3.1"
stellar-tokens = "=0.7.1"
stellar-access = "=0.7.1"
stellar-contract-utils = "=0.7.1"
stellar-macros = "=0.7.1"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true

[profile.release-with-logs]
inherits = "release"
debug-assertions = true
