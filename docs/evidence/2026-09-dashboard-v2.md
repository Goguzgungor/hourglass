# Dashboard v2 — manual checklist (testnet, wallet with ≥ 21 streams: the deployer has 65)

Run on 2026-09-17 against production (https://hourglassprotocol.org, main at `dde7d28`) with the maintainer's Freighter testnet wallet `GDAS7NL4HHEUTOBQ3TYXN6DXJP6HDBUB4DMC7ZXXE27EI3IBTDBNESIG` (124 sender streams at the time, see `2026-09-create-flow-testnet.md`), browser driven by Claude in Chrome. Items 6 and 9 were run against the local stack (`localhost:3000` on the same commit, same testnet contracts, indexer `npx tsx scripts/indexer.ts`, Mongo in Docker) because the production indexer lives in Dokploy and the production tab stayed hidden during the run.

| # | Scenario | Expected | Result |
|---|---|---|---|
| 1 | Open `/dashboard`, connect | stats strip shows sending/receiving counts and token totals; 20 rows; "Load 20 more" | ✅ Sending 124 · Receiving 1 · "0 streaming · 124 pending"; XLM card "locked 38.0 as sender / claimable 0.0 as recipient"; 20 rows (#189…#170) + "Load 20 more" |
| 2 | Role = Sending, Status = streaming, Shape = Recurring | list narrows; URL contains `role=sender&status=streaming&model=Recurring` | ✅ URL `/dashboard?role=sender&status=streaming&model=Recurring`, empty state "No streams match these filters" (nothing was streaming yet); adding Status = pending → URL `status=streaming%2Cpending` and the list narrows to #69 (the only Recurring stream) |
| 3 | Reload the page | same filters and list | ✅ same chips highlighted, same empty state after a hard reload of the URL above |
| 4 | Search `45` / a `G…` prefix / `hello` | id match / prefix matches / hint, no request | ✅ `68` → only #68 (URL `?q=68`); `GCWO` → #71 and #68 (recipient prefix), one request `/api/streams?address=…&q=GCWO&limit=20`; `hello` / `hell` → hint "Search by stream id, G… account or C… token" and **no** `/api/streams` request (checked in the network log) |
| 5 | Sort by End ascending | order flips; URL `sort=end_ts&order=asc` | ✅ URL `/dashboard?sort=end_ts&order=asc`, arrow flips to ↑, list starts with #70, #71, … (the 1-hour batch streams, earliest end) |
| 6 | Create a stream in another tab, wait ≤ 10 s | "1 new stream — show" banner; click → row at top | ✅ (local stack, tab in the foreground) stream #191 (1 XLM Linear from the deployer to this wallet, created with the stellar CLI, tx `c5c440fb6a07cea905d2a40296c88217934aba824353fcefc927995fc930f15f`) → within one 10-second poll the banner "1 new stream — show" appeared above the list; click → #191 inserted at the top, highlighted, "← from GBXD…3MU2 · ends in 7m 35s", STREAMING. Note: polling is paused while the tab is hidden (`document.visibilityState !== "visible"`), so nothing happens until the tab is in the foreground — by design. #190 (Recurring 120 × 0.01 XLM, tx `de2a25cdde3b0df843e994e0f693b3ce35d686692f56671ee2b893a3a356abf9`) was created through the production UI for item 8. |
| 7 | History tab, Only my actions off/on, kinds = Withdrawn | rows with tx links; filters apply | ✅ `?tab=history` lists "Created #189 · Linear 0.1 XLM · by you · with GBYZ…CVZC · tx ↗" rows (tx links to stellar.expert); kinds = Withdrawn → "None of the loaded 25 actions match — load more or change the kinds" + "Load 25 more"; Only my actions on → URL `?tab=history&mine=1&kinds=withdrawn` |
| 8 | Open a recurring stream with count ≥ 100 | detail page renders instantly; "and N more unlocks" caption | ✅ `/stream/190` (count 120) renders immediately; Schedule tab lists 24 unlocks then "and 96 more unlocks every 1d until Jan 14, 2027, 10:58 AM"; duration 119d |
| 9 | Indexer restart: stop the indexer, reload, start it again | "Indexer offline?" panel while it is down; list recovers within 10 s of the restart, panel clears | ✅ local stack (`docker stop id-mongodb-1` → API answers `db unreachable: connect ECONNREFUSED …:27017`): within one poll the dashboard showed the "Indexer offline?" panel ("Could not reach the indexer API. Make sure the indexer is running (npx tsx scripts/indexer.ts) and that Mongo is up." + the error text) while keeping the last list; on a fresh load with Mongo down the stats strip shows "—". `docker start id-mongodb-1` → the API answered within 2 s, the panel cleared and the list/stats resumed within ~12 s without a reload; the local indexer (`npx tsx scripts/indexer.ts`) reconnected on its own and caught up. |

Observations (not blocking): the footer still says `V0.1.0-MVP`; a self-stream (#66, sender = recipient) reads "by you · with GDAS…ESIG" in History; the two console errors on localhost come from the Phantom / EVM wallet extensions, not from the app.
